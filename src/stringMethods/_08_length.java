package stringMethods;

public class _08_length {
    public static void main(String[] args) {
        /*
        Method Task: count the total number of the characters in a String and return it as an int
        -It is non-static method, because it is called with object name
        -It is a return type method. you can store it or directly out into a System.out.println();
                and it returns an int() (total number of characters)
         Is counting HOW MANY, so it starts with 1.
         */
        String city = "Chicago";

        System.out.println(city.length());
        System.out.println(city.length() + 3); // 10

        String company = "TechGlobal";

       int size = company.length(); // 10 it






    }
}
